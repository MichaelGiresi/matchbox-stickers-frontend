import React, { useEffect } from 'react'
import './shop-all.css'

const ShopAll = () => {
    // make a get request with all products
    // display all products using display grid
    // make each product clickable, and take them to the product page

    useEffect(() => {
        const url = ``
    })
    
  return (
    <div className='shopall-container'>
        <div></div>
    </div>
  )
}

export default ShopAll